test.each([['fooanddnayo', 'anddna']])(
    '5. Longest Palindromic Substring',
    (input, expected) => {
        const longestPalindrome = s => {
            return undefined;
        };

        const result = longestPalindrome(input);
        expect(result).toEqual(expected);
    }
);
